description "Method 1 Node"

stop on (stopping meth1)
instance $ODIRECT,$FILE
setuid ubuntu

script
  MEM=$( free | head -2 | tail -1 | sed -e 's/\ \+/ /g' | cut -d' ' -f2 )
  RMEM=$(( $MEM / 100 ))
  D=$( date )
  echo "# ${D}" >> /home/ubuntu/bad-node.log
  meth1_node $RMEM 9000 $ODIRECT $FILE >> /home/ubuntu/bad-node.log
end script

